package util;
/* 

1. Math.abs(x) - Valor absoluto
   Devuelve el valor absoluto de un número.

   Ejemplo:
   double result = Math.abs(-5.5);
   System.out.println(result);  // Salida: 5.5

2. Math.pow(base, exponente) - Potencia
   Devuelve el valor de base elevado a la potencia del exponente.

   Ejemplo:
   double result = Math.pow(2, 3);
   System.out.println(result);  // Salida: 8.0

3. Math.sqrt(x) - Raíz cuadrada
   Devuelve la raíz cuadrada de un número.

   Ejemplo:
   double result = Math.sqrt(16);
   System.out.println(result);  // Salida: 4.0

4. Math.ceil(x) - Redondeo hacia arriba
   Redondea un número hacia el siguiente valor entero más cercano.

   Ejemplo:
   double result = Math.ceil(4.3);
   System.out.println(result);  // Salida: 5.0

5. Math.floor(x) - Redondeo hacia abajo
   Redondea un número hacia el valor entero más cercano, pero hacia abajo.

   Ejemplo:
   double result = Math.floor(4.7);
   System.out.println(result);  // Salida: 4.0

6. Math.round(x) - Redondeo
   Redondea el número al valor entero más cercano.

   Ejemplo:
   long result = Math.round(4.5);
   System.out.println(result);  // Salida: 5

7. Math.max(a, b) - Máximo entre dos números
   Devuelve el número mayor entre los dos valores.

   Ejemplo:
   int result = Math.max(5, 10);
   System.out.println(result);  // Salida: 10

8. Math.min(a, b) - Mínimo entre dos números
   Devuelve el número menor entre los dos valores.

   Ejemplo:
   int result = Math.min(5, 10);
   System.out.println(result);  // Salida: 5

9. Math.random() - Número aleatorio
   Devuelve un número aleatorio entre 0.0 (inclusive) y 1.0 (exclusive).

   Ejemplo:
   double result = Math.random();
   System.out.println(result);  // Salida: Un número aleatorio entre 0.0 y 1.0

10. Math.log(x) - Logaritmo natural
    Devuelve el logaritmo natural (base e) de un número.

    Ejemplo:
    double result = Math.log(10);
    System.out.println(result);  // Salida: 2.302585

11. Math.sin(x) - Seno
    Devuelve el seno de un ángulo en radianes.

    Ejemplo:
    double result = Math.sin(Math.PI / 2);
    System.out.println(result);  // Salida: 1.0

12. Math.cos(x) - Coseno
    Devuelve el coseno de un ángulo en radianes.

    Ejemplo:
    double result = Math.cos(Math.PI);
    System.out.println(result);  // Salida: -1.0

13. Math.tan(x) - Tangente
    Devuelve la tangente de un ángulo en radianes.

    Ejemplo:
    double result = Math.tan(Math.PI / 4);
    System.out.println(result);  // Salida: 1.0

*/